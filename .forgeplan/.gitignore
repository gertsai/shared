# Forgeplan derived/cache files — not committed.
# Source of truth: markdown в prds/, rfcs/, adrs/, specs/, epics/, evidence/, problems/, solutions/, refresh/
# + state YAML в state/

lance/
logs/
.lock
memory/
discovery/
notes/
