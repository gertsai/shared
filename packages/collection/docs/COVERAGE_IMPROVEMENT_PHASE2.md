# 🏗️ Архитектурный план улучшения покрытия - Фаза 2

## 🎯 Цель: Достичь 90% покрытия

### Текущее состояние:

- **Общее покрытие**: 85.83%
- **Целевое покрытие**: 90%

## 📊 Анализ файлов с низким покрытием

### 1. **MutableCollection.ts** (75.95% → 90%)

**Архитектурная классификация**: Core/Trunk - критический компонент

#### Непокрытые области:

- Строки 151, 418-422, 425-429: Методы трансформации
- Строки 448-458, 502-521: Операции поиска и фильтрации
- Строки 526-541, 550-564: Агрегатные операции
- Строки 577-584, 606-612: Специализированные операции
- Строки 615-640, 656-660: Вспомогательные методы

#### План тестирования:

```typescript
// Необходимые тесты:
- mapArray() с различными трансформациями
- flatMapArray() с вложенными массивами
- filterIter(), takeIter(), skipIter() - lazy операции
- groupBy() с различными ключами группировки
- partition() с различными предикатами
- reduce() с различными аккумуляторами
- scan() для промежуточных результатов
```

### 2. **helpers.ts** (76.11% → 90%)

**Архитектурная классификация**: Utils/Leaf - можно свободно тестировать

#### Непокрытые области:

- Строки 39-46, 52-58: isCollection проверки
- Строки 64-65, 71-74: isPlainObject edge cases
- Строки 80-100, 106-109: deepMerge сложные случаи
- Строки 115-131, 195-198: deepClone edge cases
- Строки 283-300, 313-318: специальные утилиты

#### План тестирования:

```typescript
// Необходимые тесты:
- isCollection() с различными типами объектов
- isPlainObject() с классами и прототипами
- deepMerge() с циклическими ссылками
- deepClone() с WeakMap/WeakSet
- ensureIterable() с генераторами
- fromPairs() с дубликатами ключей
```

### 3. **memoize.ts** (73.64% → 90%)

**Архитектурная классификация**: Utils/Leaf - оптимизационный компонент

#### Непокрытые области:

- Строки 50-51, 83-84: Cache invalidation
- Строки 141-142, 178: WeakMap кеширование
- Строки 187-201: Сложные мемоизации
- Строки 227-229, 231: Edge cases
- Строки 248-252, 282-287: Очистка кеша
- Строки 330-335, 351-355: Статистика кеша
- Строки 370-382, 393-435: Продвинутые стратегии
- Строки 451-472: TTL и размерные ограничения

#### План тестирования:

```typescript
// Необходимые тесты:
- memoize() с TTL expiration
- memoizeWeak() с объектными ключами
- memoizeOne() с различными аргументами
- Cache size limits и eviction
- Cache statistics и monitoring
- Concurrent access patterns
```

### 4. **ExtendedOps.ts** (69.62% → 90%)

**Архитектурная классификация**: Mixin/Branch - расширение функциональности

#### Непокрытые области:

- Строки 93-102: randomKey edge cases
- Строки 160-214: Неиспользуемые методы миксина

#### План тестирования:

```typescript
// Необходимые тесты:
- randomKey() с пустой коллекцией
- randomKey() с отрицательным amount
- clone() для immutable коллекций
- partition() через миксин
- hasAll()/hasAny() через миксин
```

### 5. **PersistentMap.ts** (72.71% → 90%)

**Архитектурная классификация**: Core/Trunk - HAMT структура данных

#### Непокрытые области:

- Строки 105, 117, 130-134: Внутренние узлы HAMT
- Строки 137-146, 149-171: Merge операции узлов
- Строки 174-190, 193-196: Collision handling
- Строки 236-237, 255-256: Удаление узлов
- Строки 261-262, 292-298: Оптимизации узлов
- Строки 304-307, 314-315: Branch merging
- Строки 317-332, 334-337: Complex collisions
- Строки 339-372: Advanced node operations
- Строки 432-433: Edge cases

#### План тестирования:

```typescript
// Необходимые тесты:
- HAMT node splitting при росте
- HAMT node merging при удалении
- Hash collision resolution
- Deep tree rebalancing
- Memory efficiency tests
- Concurrent modifications
- Large dataset operations (100k+ items)
```

## 🏗️ Архитектурная стратегия улучшения

### Приоритизация по архитектурным слоям:

1. **Core/Trunk (Критично)**:
   - PersistentMap.ts - HAMT структура
   - MutableCollection.ts - базовая функциональность

2. **Branch (Важно)**:
   - ExtendedOps.ts - расширения функциональности

3. **Leaf (Желательно)**:
   - helpers.ts - утилиты
   - memoize.ts - оптимизации

### Стратегия тестирования по PEGASUS-5:

#### WHAT (Требования):

- Достичь 90% покрытия кода
- Обеспечить тестирование всех критических путей
- Валидировать edge cases и error handling

#### SUCCESS (Критерии успеха):

- Все core компоненты > 85% покрытия
- Все branch компоненты > 80% покрытия
- Все leaf компоненты > 75% покрытия
- 0 failing tests
- Performance benchmarks не деградируют

#### CONSTRAINTS (Ограничения):

- Не изменять публичный API
- Сохранить обратную совместимость
- Не увеличивать сложность O(n)
- Тесты должны выполняться < 5 секунд

#### TESTS (Стратегия верификации):

- Unit tests для изолированных компонентов
- Integration tests для миксинов
- Property-based tests для HAMT операций
- Performance tests для больших датасетов

## 📝 План реализации

### Фаза 1: Core Components (13 story points)

1. **PersistentMap.ts** - HAMT внутренности (8 SP)
2. **MutableCollection.ts** - недостающие методы (5 SP)

### Фаза 2: Branch Components (5 story points)

1. **ExtendedOps.ts** - edge cases (5 SP)

### Фаза 3: Leaf Components (8 story points)

1. **helpers.ts** - утилиты (3 SP)
2. **memoize.ts** - кеширование (5 SP)

## 🔒 Защитные механизмы

### Boundary Protection:

- Core файлы требуют review архитектора
- Изменения в HAMT требуют performance benchmarks
- Публичный API защищён типами и контрактами

### Quality Gates:

- Coverage не должен упасть ниже текущего
- Все новые методы должны иметь JSDoc
- Complexity metrics не должны превышать 10

## 📊 Метрики успеха

| Файл                 | Текущее | Целевое | Приоритет |
| -------------------- | ------- | ------- | --------- |
| PersistentMap.ts     | 72.71%  | 90%     | HIGH      |
| MutableCollection.ts | 75.95%  | 90%     | HIGH      |
| ExtendedOps.ts       | 69.62%  | 85%     | MEDIUM    |
| helpers.ts           | 76.11%  | 85%     | LOW       |
| memoize.ts           | 73.64%  | 85%     | LOW       |

## 🚀 Следующие шаги

1. Начать с PersistentMap.ts - самый большой потенциал улучшения
2. Добавить property-based тесты для HAMT
3. Покрыть все публичные методы MutableCollection
4. Добавить edge case тесты для утилит

## ⚠️ Риски и митигации

| Риск                          | Вероятность | Воздействие | Митигация              |
| ----------------------------- | ----------- | ----------- | ---------------------- |
| Сломать HAMT логику           | Низкая      | Высокое     | Property-based testing |
| Деградация производительности | Средняя     | Среднее     | Benchmark suite        |
| Нарушить контракты            | Низкая      | Высокое     | Type checking          |
| Увеличить сложность           | Средняя     | Низкое      | Code review            |
